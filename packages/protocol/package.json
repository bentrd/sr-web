{
	"name": "@sr-web/protocol",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"dev": "echo 'protocol: types-only, no dev step'",
		"build": "echo 'protocol: types-only, nothing to build'",
		"typecheck": "tsc --noEmit"
	}
}
