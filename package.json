{
	"name": "sr-web",
	"private": true,
	"type": "module",
	"workspaces": ["apps/*", "packages/*"],
	"scripts": {
		"dev": "bun run --filter '*' dev",
		"build": "bun run --filter '*' build",
		"typecheck": "bun run --filter '*' typecheck",
		"collect-maps": "bun run scripts/collect-maps.ts"
	},
	"devDependencies": {
		"typescript": "^5.6.3"
	}
}
